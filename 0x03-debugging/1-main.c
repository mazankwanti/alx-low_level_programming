#include <stdio.h>
/**
 *Return: 0
 */
int main(void)
{
int i;
printf("infinite loop incoming :("\n");

1 = 0;

/*
 * while (i = 10);
 * infite loop = noNo increase of variable
 * putchar (0);
 * }
 */
printF("infinite loop  avoided! //o\n");
return (0);
}
